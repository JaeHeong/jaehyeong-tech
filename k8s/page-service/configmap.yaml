apiVersion: v1
kind: ConfigMap
metadata:
  name: page-service-config
  namespace: msa-services
data:
  NODE_ENV: "production"
  PORT: "3004"
  RABBITMQ_URL: "amqp://admin:admin@rabbitmq-service:5672"
  # Database URL will be set in deployment
