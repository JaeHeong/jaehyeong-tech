apiVersion: apps/v1
kind: Deployment
metadata:
  name: page-service
  namespace: jaehyeong-tech-dev
  labels:
    app: page-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: page-service
  template:
    metadata:
      labels:
        app: page-service
    spec:
      containers:
        - name: page-service
          image: ghcr.io/jaehyeong/page-service:dev-latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3004
              name: http
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: page-service-config
                  key: PORT
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: page-service-config
                  key: NODE_ENV
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: postgresql-secret
                  key: PAGE_DATABASE_URL
            - name: RABBITMQ_URL
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secret
                  key: RABBITMQ_URL
            - name: IP_HASH_SALT
              valueFrom:
                secretKeyRef:
                  name: page-service-secret
                  key: IP_HASH_SALT
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: page-service
  namespace: jaehyeong-tech-dev
  labels:
    app: page-service
spec:
  type: ClusterIP
  selector:
    app: page-service
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
      name: http
---
apiVersion: v1
kind: Secret
metadata:
  name: page-service-secret
  namespace: jaehyeong-tech-dev
type: Opaque
stringData:
  IP_HASH_SALT: "change-this-in-production"
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: page-service-hpa
  namespace: jaehyeong-tech-dev
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: page-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
