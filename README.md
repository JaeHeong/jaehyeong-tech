# Jaehyeong Tech Blog

DevOps, MLOps, 클라우드 인프라 등 기술 경험을 기록하는 개인 기술 블로그

**URL**: https://tech.jaehyeong.site

## 기술 스택

### Frontend
- **React 19** - 최신 React
- **Vite 6** - 빠른 개발 서버 및 빌드
- **TypeScript 5.7** - 타입 안전성
- **Tailwind CSS 3.4** - 반응형 UI
- **React Router 7** - 클라이언트 라우팅
- **TipTap 3** - WYSIWYG 에디터
- **Lowlight** - 코드 하이라이팅

### Backend
- **Node.js 22 LTS** - 런타임
- **Express.js 5** - 웹 프레임워크
- **TypeScript 5.7** - 타입 안전성
- **Prisma 6** - ORM
- **PostgreSQL 16** - 데이터베이스
- **Zod** - 스키마 검증
- **JWT + Google OAuth** - 인증

### Infrastructure
- **Docker & Docker Compose** - 컨테이너화
- **Traefik** - 리버스 프록시 + SSL (Cloudflare DNS)
- **Nginx** - 정적 파일 서빙
- **OCI Object Storage** - 이미지/백업 저장소 (Oracle Cloud)

## 프로젝트 구조

```
jaehyeong-tech/
├── apps/
│   ├── web/                    # React Frontend
│   │   ├── src/
│   │   │   ├── components/     # 재사용 컴포넌트
│   │   │   │   ├── TipTapEditor.tsx    # WYSIWYG 에디터
│   │   │   │   ├── Header.tsx          # 헤더 + 네비게이션
│   │   │   │   ├── Footer.tsx          # 푸터
│   │   │   │   ├── Sidebar.tsx         # 사이드바 (프로필, 카테고리)
│   │   │   │   ├── CommentSection.tsx  # 댓글 시스템
│   │   │   │   ├── Layout.tsx          # 공통 레이아웃
│   │   │   │   ├── AdminLayout.tsx     # 관리자 레이아웃
│   │   │   │   ├── ProtectedRoute.tsx  # 인증 가드
│   │   │   │   ├── MobileProfileModal.tsx  # 모바일 프로필 모달
│   │   │   │   ├── RadialWheelMenu.tsx # 관리자 빠른 메뉴
│   │   │   │   └── MouseSparkle.tsx    # 마우스 효과
│   │   │   ├── pages/          # 페이지 컴포넌트
│   │   │   │   ├── HomePage.tsx            # 메인 페이지
│   │   │   │   ├── PostListPage.tsx        # 포스트 목록
│   │   │   │   ├── PostDetailPage.tsx      # 포스트 상세
│   │   │   │   ├── CategoryPage.tsx        # 카테고리별 포스트
│   │   │   │   ├── SearchPage.tsx          # 검색 결과
│   │   │   │   ├── LoginPage.tsx           # 사용자 로그인
│   │   │   │   ├── IntroducePage.tsx       # 소개 페이지
│   │   │   │   ├── PrivacyPolicyPage.tsx   # 개인정보처리방침
│   │   │   │   ├── NoticePage.tsx          # 공지사항 목록
│   │   │   │   ├── NoticeDetailPage.tsx    # 공지사항 상세
│   │   │   │   ├── NotFoundPage.tsx        # 404 에러 페이지
│   │   │   │   ├── AdminLoginPage.tsx      # 관리자 로그인
│   │   │   │   ├── AdminDashboardPage.tsx  # 관리자 대시보드
│   │   │   │   ├── AdminPostEditorPage.tsx # 포스트 에디터
│   │   │   │   ├── AdminPostsPage.tsx      # 포스트 관리
│   │   │   │   ├── AdminDraftsPage.tsx     # 임시 저장 관리
│   │   │   │   ├── AdminDraftEditorPage.tsx# 임시 저장 편집
│   │   │   │   ├── AdminCategoriesPage.tsx # 카테고리 관리
│   │   │   │   ├── AdminTagsPage.tsx       # 태그 관리
│   │   │   │   ├── AdminPagesPage.tsx      # 페이지 관리
│   │   │   │   ├── AdminPageEditorPage.tsx # 정적 페이지 에디터
│   │   │   │   ├── AdminCommentsPage.tsx   # 댓글 관리
│   │   │   │   ├── AdminSettingsPage.tsx   # 프로필 설정
│   │   │   │   └── AdminManagementPage.tsx # 시스템 관리
│   │   │   ├── contexts/       # React Context
│   │   │   │   ├── AuthContext.tsx     # 인증 상태 관리
│   │   │   │   └── ModalContext.tsx    # 전역 모달 (confirm/alert)
│   │   │   ├── hooks/          # 커스텀 훅
│   │   │   │   └── useSEO.ts           # SEO 메타태그 관리
│   │   │   ├── services/       # API 클라이언트
│   │   │   │   └── api.ts              # API 서비스
│   │   │   ├── styles/         # 전역 스타일
│   │   │   └── types/          # 타입 정의
│   │   ├── Dockerfile
│   │   └── package.json
│   │
│   └── api/                    # Express Backend
│       ├── src/
│       │   ├── controllers/    # 요청 핸들러
│       │   │   ├── auth.ts         # 인증 (로그인, 회원가입, Google OAuth)
│       │   │   ├── posts.ts        # 포스트 CRUD + 이미지 연결
│       │   │   ├── drafts.ts       # 임시 저장 관리
│       │   │   ├── categories.ts   # 카테고리 관리
│       │   │   ├── tags.ts         # 태그 관리
│       │   │   ├── pages.ts        # 페이지 관리 (공지사항/정적 페이지)
│       │   │   ├── comments.ts     # 댓글 시스템
│       │   │   ├── likes.ts        # 좋아요 기능
│       │   │   ├── stats.ts        # 통계 API
│       │   │   ├── upload.ts       # 이미지 업로드 (OCI)
│       │   │   ├── images.ts       # 이미지 관리 (고아 이미지 정리)
│       │   │   ├── backup.ts       # 백업/복원 (OCI)
│       │   │   ├── seo.ts          # SEO (sitemap, rss, robots.txt)
│       │   │   └── metadata.ts     # URL 메타데이터 추출
│       │   ├── routes/         # API 라우트
│       │   ├── middleware/     # 미들웨어 (인증, 에러 처리)
│       │   ├── utils/          # 유틸리티
│       │   │   └── readingTime.ts  # 읽기 시간 계산
│       │   └── services/       # 비즈니스 로직
│       │       ├── prisma.ts       # Prisma 클라이언트
│       │       └── oci.ts          # OCI Object Storage 서비스
│       ├── prisma/
│       │   ├── schema.prisma   # DB 스키마
│       │   └── seed.ts         # 시드 데이터
│       ├── Dockerfile
│       └── package.json
│
├── packages/
│   └── shared/                 # 공유 타입/유틸리티
│
├── wireframes/                 # UI 와이어프레임 (HTML)
├── .env.example
├── pnpm-workspace.yaml
└── package.json                # 워크스페이스 루트
```

## 주요 기능

### 포스트 작성 (TipTap 에디터)

| 기능 | 설명 | 사용법 |
|------|------|--------|
| **제목** | H1, H2, H3 | 툴바 또는 `/1`, `/2`, `/3` |
| **텍스트 서식** | 굵게, 기울임, 취소선 | 툴바 또는 `Ctrl+B`, `Ctrl+I` |
| **인용문** | 기본 블록쿼트 | 툴바 또는 `/quote` |
| **Callout** | 노션 스타일 콜아웃 (6종) | 툴바 또는 `/callout` |
| **Pull Quote** | 큰 따옴표 강조 인용 | 툴바 또는 `/pullquote` |
| **코드 블록** | 30+ 언어 구문 강조 | 툴바 또는 `/code` |
| **리스트** | 순서/비순서 목록 | 툴바 또는 `/ul`, `/ol` |
| **링크** | 일반 링크 삽입 | 툴바 |
| **북마크** | 노션 스타일 링크 카드 | 링크 모달 → 북마크 버튼 |
| **이미지** | 드래그 앤 드롭 업로드 | 툴바 (OCI 저장) |
| **YouTube** | 영상 임베드 | 툴바 |
| **구분선** | 가로선 | `/hr` |

### Callout 타입

| 타입 | 아이콘 | 용도 |
|------|--------|------|
| info | 💡 | 정보 안내 |
| warning | ⚠️ | 주의 사항 |
| error | 🚨 | 오류/위험 |
| success | ✅ | 성공/완료 |
| note | 📝 | 메모 |
| tip | 🔥 | 유용한 팁 |

### 관리자 기능

- [x] JWT 기반 인증
- [x] Google OAuth 로그인
- [x] 포스트 작성/수정/삭제
- [x] **임시 저장 기능** (자동 저장, 임시 저장 목록)
- [x] 미리보기 모달 (ESC로 닫기)
- [x] 저장하지 않은 변경사항 경고
- [x] 대시보드 통계 (포스트, 조회수, 좋아요, 댓글)
- [x] **카테고리 관리** (CRUD, 아이콘/색상, 포스트 수 표시)
- [x] **태그 관리** (CRUD, 인라인 편집, 포스트 수 표시)
- [x] 공지사항 관리 (작성/수정/삭제, 뱃지, 상단 고정)
- [x] 정적 페이지 관리 (에디터, 라이브 프리뷰)
- [x] **댓글 관리** (조회, 삭제, 비밀 댓글 확인)
- [x] **프로필 설정** (이름, 아바타, 소셜 링크)
- [x] **데이터 백업/복원** (OCI Object Storage, 미리보기)
- [x] **고아 이미지 정리** (DB 기반 추적)

### 사용자 기능

- [x] 반응형 디자인 (모바일/태블릿/데스크톱)
- [x] 다크모드 지원
- [x] 포스트 목록/상세 보기
- [x] 카테고리별 필터링
- [x] 태그별 필터링
- [x] 전체 검색
- [x] 조회수 표시 (IP 기반 중복 방지)
- [x] 예상 읽기 시간
- [x] **좋아요 기능** (로그인/비로그인 모두 가능)
- [x] **댓글 시스템** (익명/회원, 대댓글, 비밀 댓글)
- [x] 공지사항 (뱃지, 고정 공지, 페이지네이션, 이전/다음 글)
- [x] 정적 페이지 (소개, 개인정보처리방침)
- [x] **커스텀 모달** (confirm/alert)
- [x] **모바일 메뉴** (터치 외부 닫기, 애니메이션)

### 댓글 시스템

```
댓글 작성 (익명 또는 로그인)
     ↓
익명: 닉네임 + 비밀번호 입력 → 수정/삭제 시 비밀번호 확인
회원: 로그인 상태로 작성 → 본인만 수정/삭제
     ↓
대댓글 지원 (1단계 깊이)
비밀 댓글 지원 (작성자 + 관리자만 확인)
```

### 이미지 관리

이미지는 OCI Object Storage에 저장되며, DB에서 추적됩니다:

```
이미지 업로드 → DB에 메타데이터 저장 (postId = null)
     ↓
글 저장 → 본문의 이미지 URL과 매칭하여 postId 연결
     ↓
글 삭제 → 연결된 이미지 OCI에서 즉시 삭제

고아 이미지 (postId = null, 24시간 경과) → 관리 페이지에서 정리
```

### SEO

- [x] `/sitemap.xml` - 동적 사이트맵 생성
- [x] `/rss.xml` - RSS 피드
- [x] `/robots.txt` - 검색 엔진 크롤러 설정

## 시작하기

### 사전 요구사항

- Node.js 22+
- pnpm 9+
- Docker & Docker Compose

### 로컬 개발

```bash
# 저장소 클론
git clone https://github.com/JaeHeong/jaehyeong-tech.git
cd jaehyeong-tech

# 의존성 설치
pnpm install

# 환경 변수 설정
cp .env.example .env

# PostgreSQL 시작 (Docker) - 로컬 개발용
docker run -d --name tech-db \
  -e POSTGRES_USER=techblog \
  -e POSTGRES_PASSWORD=techblog \
  -e POSTGRES_DB=jaehyeong_tech \
  -p 5433:5432 \
  postgres:16-alpine

# 데이터베이스 마이그레이션
pnpm --filter api db:push
pnpm --filter api db:seed

# 개발 서버 실행
pnpm dev
```

### 프로덕션 배포

```bash
# /home/ubuntu/n8n 디렉토리에서 실행
cd /home/ubuntu/n8n

# 블로그 서비스 빌드 및 실행
docker compose up -d --build tech-web tech-api

# DB 마이그레이션 (최초 1회)
docker compose exec tech-api npx prisma migrate deploy

# 로그 확인
docker compose logs -f tech-web tech-api
```

### 개발 서버 포트

| 서비스 | URL |
|--------|-----|
| Frontend | http://localhost:5173 |
| Backend API | http://localhost:3000 |
| PostgreSQL | localhost:5433 |

## API 엔드포인트

### 인증
| 메서드 | 경로 | 설명 |
|--------|------|------|
| POST | `/api/auth/login` | 로그인 |
| POST | `/api/auth/google` | Google OAuth |
| POST | `/api/auth/register` | 회원가입 |
| GET | `/api/auth/me` | 현재 사용자 정보 |
| PUT | `/api/auth/me` | 프로필 수정 |
| GET | `/api/author` | 블로그 작성자 정보 (공개) |

### 포스트
| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/api/posts` | 포스트 목록 (페이지네이션, 필터) |
| GET | `/api/posts/featured` | 추천 포스트 |
| GET | `/api/posts/:slug` | 포스트 상세 |
| POST | `/api/posts` | 포스트 작성 (인증) |
| PUT | `/api/posts/:id` | 포스트 수정 (인증) |
| DELETE | `/api/posts/:id` | 포스트 삭제 (인증) |
| DELETE | `/api/posts/bulk` | 포스트 일괄 삭제 (인증) |

### 임시 저장
| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/api/drafts` | 임시 저장 목록 (인증) |
| GET | `/api/drafts/:id` | 임시 저장 상세 (인증) |
| POST | `/api/drafts` | 임시 저장 생성 (인증) |
| PUT | `/api/drafts/:id` | 임시 저장 수정 (인증) |
| DELETE | `/api/drafts/:id` | 임시 저장 삭제 (인증) |

### 좋아요
| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/api/likes/:postId/status` | 좋아요 상태 확인 |
| POST | `/api/likes/:postId/toggle` | 좋아요 토글 |

### 댓글
| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/api/comments/post/:postId` | 포스트 댓글 목록 |
| GET | `/api/comments/admin` | 전체 댓글 목록 (인증) |
| POST | `/api/comments` | 댓글 작성 |
| PUT | `/api/comments/:id` | 댓글 수정 |
| DELETE | `/api/comments/:id` | 댓글 삭제 |

### 카테고리
| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/api/categories` | 카테고리 목록 |
| GET | `/api/categories/:slug/posts` | 카테고리별 포스트 |
| POST | `/api/categories` | 카테고리 생성 (인증) |
| PUT | `/api/categories/:id` | 카테고리 수정 (인증) |
| DELETE | `/api/categories/:id` | 카테고리 삭제 (인증) |

### 태그
| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/api/tags` | 태그 목록 |
| GET | `/api/tags/:slug/posts` | 태그별 포스트 |
| POST | `/api/tags` | 태그 생성 (인증) |
| PUT | `/api/tags/:id` | 태그 수정 (인증) |
| DELETE | `/api/tags/:id` | 태그 삭제 (인증) |

### 페이지 (공지사항 & 정적 페이지)
| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/api/pages` | 전체 페이지 목록 |
| GET | `/api/pages/notices` | 공지사항 목록 |
| GET | `/api/pages/notices/:slug/adjacent` | 이전/다음 공지사항 |
| GET | `/api/pages/slug/:slug` | 슬러그로 페이지 조회 |
| GET | `/api/pages/admin` | 관리자용 페이지 목록 (인증) |
| GET | `/api/pages/admin/stats` | 페이지 통계 (인증) |
| GET | `/api/pages/admin/:id` | 페이지 상세 (인증) |
| POST | `/api/pages` | 페이지 생성 (인증) |
| PUT | `/api/pages/:id` | 페이지 수정 (인증) |
| DELETE | `/api/pages/:id` | 페이지 삭제 (인증) |

### 백업 & 이미지 관리
| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/api/backups` | 백업 목록 (인증) |
| POST | `/api/backups` | 백업 생성 (인증) |
| GET | `/api/backups/:fileName` | 백업 다운로드 (인증) |
| GET | `/api/backups/:fileName/info` | 백업 정보/미리보기 (인증) |
| POST | `/api/backups/:fileName/restore` | 백업 복원 (인증) |
| DELETE | `/api/backups/:fileName` | 백업 삭제 (인증) |
| GET | `/api/images/orphans` | 고아 이미지 목록 (인증) |
| DELETE | `/api/images/orphans` | 고아 이미지 삭제 (인증) |

### 통계
| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/api/stats` | 대시보드 통계 (인증) |

### SEO
| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/sitemap.xml` | 사이트맵 |
| GET | `/rss.xml` | RSS 피드 |
| GET | `/robots.txt` | 로봇 설정 |

### 기타
| 메서드 | 경로 | 설명 |
|--------|------|------|
| POST | `/api/upload/image` | 이미지 업로드 (인증) |
| POST | `/api/metadata` | URL 메타데이터 추출 (인증) |

## 스크립트

```bash
# 전체 개발 서버 실행
pnpm dev

# Frontend만 실행
pnpm --filter web dev

# Backend만 실행
pnpm --filter api dev

# 빌드
pnpm build

# 타입 체크
pnpm --filter web typecheck
pnpm --filter api typecheck

# DB 명령어
pnpm --filter api db:migrate    # 마이그레이션 생성
pnpm --filter api db:push       # 스키마 푸시
pnpm --filter api db:seed       # 시드 데이터
pnpm --filter api db:studio     # Prisma Studio (DB GUI)
```

## 환경 변수

```env
# Database
DATABASE_URL=postgresql://techblog:techblog@localhost:5433/jaehyeong_tech

# Backend
PORT=3000
NODE_ENV=development
JWT_SECRET=your-secret-key
CORS_ORIGIN=http://localhost:5173
IP_HASH_SALT=your-ip-hash-salt

# Google OAuth (선택)
GOOGLE_CLIENT_ID=your-google-client-id

# OCI Object Storage (프로덕션)
OCI_TENANCY=your-tenancy-ocid
OCI_USER=your-user-ocid
OCI_FINGERPRINT=your-api-key-fingerprint
OCI_PRIVATE_KEY=your-private-key
OCI_REGION=ap-chuncheon-1
OCI_BUCKET_NAME=jaehyeong-tech-uploads
OCI_BACKUP_BUCKET_NAME=jaehyeong-tech-backups

# Frontend
VITE_API_URL=http://localhost:3000
VITE_GOOGLE_CLIENT_ID=your-google-client-id
```

## 데이터베이스 스키마

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│    User     │     │    Post     │     │  Category   │
├─────────────┤     ├─────────────┤     ├─────────────┤
│ id          │────<│ authorId    │     │ id          │
│ email       │     │ categoryId  │>────│ name        │
│ password    │     │ slug        │     │ slug        │
│ googleId    │     │ title       │     │ description │
│ name        │     │ content     │     │ icon        │
│ avatar      │     │ excerpt     │     │ color       │
│ bio         │     │ coverImage  │     └─────────────┘
│ title       │     │ viewCount   │
│ github      │     │ likeCount   │     ┌─────────────┐
│ twitter     │     │ readingTime │     │    Tag      │
│ linkedin    │     │ status      │     ├─────────────┤
│ website     │     │ featured    │>───<│ id          │
│ role        │     │ publishedAt │     │ name        │
└──────┬──────┘     └──────┬──────┘     │ slug        │
       │                   │            └─────────────┘
       │            ┌──────┴──────┐
       │            │   Image     │     ┌─────────────┐
       │            ├─────────────┤     │   Comment   │
       │            │ id          │     ├─────────────┤
       │            │ url         │     │ id          │
       │            │ objectName  │     │ content     │
       │            │ filename    │     │ postId      │
       │            │ size        │     │ authorId    │
       │            │ postId (FK) │     │ guestName   │
       │            └─────────────┘     │ guestPassword│
       │                                │ parentId    │
       │            ┌─────────────┐     │ isPrivate   │
       │            │    Like     │     │ isDeleted   │
       │            ├─────────────┤     └─────────────┘
       │            │ id          │
       │            │ postId      │     ┌─────────────┐
       │            │ userId      │     │   Draft     │
       │            │ ipHash      │     ├─────────────┤
       │            └─────────────┘     │ id          │
       │                                │ title       │
       │            ┌─────────────┐     │ content     │
       │            │  PostView   │     │ excerpt     │
       │            ├─────────────┤     │ coverImage  │
       │            │ id          │     │ categoryId  │
       │            │ postId      │     │ tagIds[]    │
       │            │ ipHash      │     │ authorId    │
       │            └─────────────┘     └─────────────┘
       │
       │            ┌─────────────┐
       │            │    Page     │
       │            ├─────────────┤
       └───────────<│ authorId    │
                    │ slug        │
                    │ title       │
                    │ type        │  (STATIC/NOTICE)
                    │ content     │  (HTML/Markdown)
                    │ status      │
                    │ badge       │
                    │ isPinned    │
                    │ template    │
                    └─────────────┘
```

## 버전 히스토리

### v1.0.0 (2025-01-08)
- 최초 릴리즈
- 포스트 작성/수정/삭제 (TipTap 에디터)
- 카테고리/태그 관리
- 댓글 시스템 (익명/회원, 대댓글, 비밀 댓글)
- 좋아요 기능
- 임시 저장 기능
- 공지사항 및 정적 페이지 관리
- Google OAuth 로그인
- OCI Object Storage 이미지 저장
- 데이터 백업/복원
- 고아 이미지 정리
- SEO (sitemap, rss, robots.txt)
- 반응형 디자인 (모바일/태블릿/데스크톱)
- 다크모드 지원
- 커스텀 모달 (confirm/alert)

## 라이선스

MIT License
